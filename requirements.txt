# deepMRI – Python dependencies
# Install with:   pip install -r requirements.txt
#
# NOTE: The torch line below installs the CPU-only wheel, which works on
# Apple Silicon (ARM64) and Intel/AMD machines without a GPU.
# On a Linux server with an NVIDIA GPU, replace it with:
#   torch>=2.0.0  (and use the pytorch/pytorch Docker base image)

--index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0

--index-url https://pypi.org/simple
numpy>=1.23
scipy>=1.10
nibabel>=5.0
matplotlib>=3.7
gradio>=4.0
